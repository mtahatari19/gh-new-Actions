name: Ping on Raspi
on: [push, workflow_dispatch]

jobs:
  ping:
    runs-on: [self-hosted, Linux, ARM]  
    steps:
      - name: Show env
        run: |
          uname -a
          echo "Hello from Raspberry Pi (ARM)!"
